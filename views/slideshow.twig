<div class="owl-slideshow-wp">
  {% if slideshow_title %}
    <h2 class="owl-title">{{ slideshow_title|e }}</h2>
  {% endif %}
  <div class="gallery owl-carousel">
    {% for image in images %}
      <div class="item">
        <img src="{{ image.src(size) }}" alt="">
        {% if image.caption %}
          <p class="wp-caption-text">
            {{ image.caption }}
          </p>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>